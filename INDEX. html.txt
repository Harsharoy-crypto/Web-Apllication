<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flipkart Login</>
    <style>
        /* Basic Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .login-container {
            background-color: white;
            width: 400px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #2874f0;
        }

        .input-field {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 14px;
            outline: none;
        }

        .input-field:focus {
            border-color: #2874f0;
            box-shadow: 0 0 5px rgba(40, 116, 240, 0.2);
        }

        .login-btn {
            width: 100%;
            padding: 12px;
            background-color: #2874f0;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .login-btn:hover {
            background-color: #1a5db8;
        }

        .forgot-password {
            margin-top: 10px;
            font-size: 12px;
            color: #2874f0;
            cursor: pointer;
            text-decoration: none;
        }

        .signup-link {
            margin-top: 20px;
            font-size: 14px;
            color: #555;
        }

        .signup-link a {
            color: #2874f0;
            text-decoration: none;
        }

        .secure-note {
            margin-top: 20px;
            font-size: 12px;
            color: #555;
        }
    </style>
</head>

<body>

    <div class="login-container">
        <h1>Welcome Back to Flipkart!</h1>
        <form>
            <input type="text" class="input-field" id="email" placeholder="Email/Phone Number" required>
            <input type="password" class="input-field" id="password" placeholder="Password" required>
            <button type="submit" class="login-btn">Login</button>
        </form>
        <a href="#" class="forgot-password">Forgot Password?</a>

        <div class="signup-link">
            New to Flipkart? <a href="#">Create Account</a>
        </div>

        <div class="secure-note">
            Flipkart uses the latest encryption technology to keep your information safe.
        </div>
    </div>

</body>

</html>

